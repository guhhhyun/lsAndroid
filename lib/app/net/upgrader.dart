/*
import 'package:flutter/material.dart';
import 'package:get/get.dart';
import 'package:lsandroid/app/pages/rackIpgo/rack_ipgo_controller.dart';
import 'package:upgrader/upgrader.dart';

class Upgrader extends StatefulWidget {
  Upgrader({super.key});

  RackIpgoController controller = Get.find();

  @override
  State<Upgrader> createState() => _UpgraderState();

  static clearSavedSettings() async{
    Get.dialog(alert());

  }

}

Widget alert() {
  RackIpgoController controller = Get.find();
  return Container(
    child: TextButton(onPressed: () async{
      for(var i in controller.registRackIpgoList) {

      }
      await Upgrader.clearSavedSettings();
      UpgradeAlert(*/
/**//*

        child: Container(
          child: Row(
            children: [
              Container(

              ),
              Container(
                decoration: BoxDecoration(),
              )
            ],
          ),
        ),
      );
      UpgradeAlert(
          child: Scaffold(
            appBar: AppBar(title: const Text('업그레이드 예제')),
            body: const Center(child: Text('업그레이드 해주세요')),
          )
      );
    }, child: Container()),
  );
}


class _UpgraderState extends State<Upgrader> {


  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

*/
